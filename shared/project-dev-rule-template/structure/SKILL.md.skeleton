---
name: project-dev-rule
description: "Use when the agent is writing, modifying, reviewing, or questioning any project code. Triggers on: [AI根据项目上下文填写]."
allowed-tools:
  - Read
  - Grep
  - Glob
  - Bash
metadata:
  version: 1.0.0
  generated_at: "{{GENERATED_AT}}"
  template_version: "{{TEMPLATE_VERSION}}"
  project_name: "{{PROJECT_NAME}}"
---

# 项目开发规范

## 快速开始

**做什么**：[AI根据项目上下文填写：一句话描述技能解决的问题]
**怎么触发**：[AI根据项目上下文填写：自动触发条件]
**前置条件**：[AI根据项目上下文填写：需要的环境或权限]
**第一次用**：[AI根据项目上下文填写：首次使用步骤]

## 核心原则

[AI根据项目上下文生成3-5条核心原则]

1. **[原则1]** — [具体说明]
2. **[原则2]** — [具体说明]
3. **[原则3]** — [具体说明]

## YAGNI 检查（借鉴 Ponytail）

在编写代码前，必须通过 YAGNI 梯队检查：

```
1. 这个功能需要存在吗？→ 不需要：跳过（YAGNI）
2. 标准库能做吗？→ 用它
3. 原生平台特性？→ 用它
4. 已安装的依赖？→ 用它
5. 能一行搞定吗？→ 一行
6. 只有那时：写最小可工作代码
```

### YAGNI 规则

- 不写推测性需求的代码
- 不重复造轮子（标准库 > 原生平台 > 已安装依赖 > 自实现）
- 不过度抽象（没有请求的抽象不做）
- 不写样板代码（"为了以后"的代码不做）
- 删除优先于添加
- 无聊优先于聪明

### YAGNI 标记

对于刻意的简化，使用 `yagni:` 注释标记：
```python
# yagni: 使用缓存，但如果性能不足需要添加缓存策略
@lru_cache(maxsize=128)
def get_data(key):
    ...
```

### YAGNI 例外

以下情况**不做** YAGNI 简化：
- 信任边界的数据验证
- 防止数据丢失的错误处理
- 安全措施
- 无障碍访问基础
- 用户明确请求的功能

## 强度级别

借鉴 Ponytail 的强度级别系统，支持 4 个级别：

| 级别 | 说明 | 使用场景 |
|------|------|----------|
| **lite** | 基础规范，适合快速开发 | 原型开发、快速验证 |
| **full** | 完整规范，适合生产环境 | 日常开发、团队协作 |
| **ultra** | 严格规范，适合关键系统 | 金融、医疗、安全关键系统 |
| **off** | 关闭规范检查 | 紧急修复、临时调试 |

### 级别切换

在 CLAUDE.md 中配置默认级别：
```markdown
## 项目开发规范
- 强度级别：full
```

或在会话中切换：
```
/project-dev-rule lite
/project-dev-rule full
/project-dev-rule ultra
/project-dev-rule off
```

### 级别行为

| 级别 | 行为 |
|------|------|
| **lite** | 只检查核心规范，跳过可选检查 |
| **full** | 检查所有规范，包括可选检查 |
| **ultra** | 检查所有规范，严格零容忍 |
| **off** | 跳过所有规范检查 |

## 何时使用

- [场景1]
- [场景2]
- [场景3]

## 目录结构

```
project-dev-rule/
├── SKILL.md              ← 主入口（本文件）
├── scripts/              ← 可执行检查脚本
├── references/           ← 详细文档（渐进式加载）
│   ├── backend/          ← 后端规范
│   ├── frontend/         ← 前端规范
│   └── business/         ← 业务规范
└── evaluations/          ← 评估场景
```

## 后端规范

[AI根据项目技术栈生成后端规范]

### 分层规范

[生成Controller/Service/Repository等分层规范]

### 命名规范

[生成类名、方法名、变量名命名规范]

### 异常处理

[生成异常处理规范]

### 日志规范

[生成日志规范]

### 测试规范

[生成测试规范]

## 前端规范

[AI根据项目技术栈生成前端规范]

### 组件规范

[生成组件文件结构、命名、props设计规范]

### 状态管理

[生成状态管理规范]

### 样式规范

[生成样式规范]

### 性能优化

[生成性能优化规范]

### 测试规范

[生成测试规范]

## 业务规范

[AI根据项目业务生成业务规范]

### 术语表

[生成项目核心术语]

### 模块结构

[生成业务模块划分]

### 通用模式

[生成通用业务模式]

## 验证清单

[AI根据项目规范生成验证清单]

### 开发前检查

- [ ] 检查项1
- [ ] 检查项2
- [ ] 检查项3

### 代码审查检查

- [ ] 检查项1
- [ ] 检查项2
- [ ] 检查项3

## 红线规则

[AI根据项目规范生成红线规则，绝对禁止的行为]

### 绝对禁止

- [禁止行为1]
- [禁止行为2]
- [禁止行为3]

### 必须遵守

- [必须行为1]
- [必须行为2]
- [必须行为3]

## 常见错误

[AI根据项目经验生成常见错误和解决方案]

## 参考

- [共享最佳实践](../shared/agent-skills-best-practices.md)
- [质量门禁工作流](../../quality-gate-workflow/SKILL.md)

## 版本记录

- **v1.0.0**（当前）：初始生成
